#include <stdio.h>
#include <ncurses.h> 
#include <string.h>

void fallingBlock(){
    switch(getch()){
        case 'A':
            //left
            break;
        case 'D':
            //right
            break;
        case 'W':
            //rotation
        }
}